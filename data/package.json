{
  "name": "data",
  "description": "Data loader into firebase",
  "scripts": {
    "build": "rm -rf dist && tsc",
    "start": "ts-node src/index.ts",
    "start:js": "node dist/index.js",
    "prepare": "npm run build"
  },
  "dependencies": {
    "firebase-admin": "^5.13.1",
    "fs-extra": "^7.0.0",
    "geokit": "^0.1.3"
  },
  "private": true,
  "devDependencies": {
    "ts-node": "^7.0.1",
    "typescript": "^3.1.1"
  }
}
